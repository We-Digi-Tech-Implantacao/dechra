{
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "gulp-sass": "^4.1.0"
  },
  "devDependencies": {
    "gulp-clean-css": "^4.3.0",
    "gulp-autoprefixer": "^7.0.1"
  },
  "browserslist": [
    "safari >= 7",
    "ios >= 7"
  ],
  "scripts": {
    "v-log": "vtex login idomed",
    "v-run": "vtex unlink --all && vtex link --clean",
    "v-gulp": "gulp mount && gulp watch"
  }
}
